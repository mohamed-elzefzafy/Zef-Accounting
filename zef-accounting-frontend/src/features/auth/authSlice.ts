// import { createSlice } from '@reduxjs/toolkit';
// const initialState = { user: null, ready: false };
// const slice = createSlice({ name: 'auth', initialState, reducers: { setUser(state, action){ state.user = action.payload; state.ready = true }, clearUser(state){ state.user = null; state.ready = true } } });
// export const { setUser, clearUser } = slice.actions; export default slice.reducer;
